# @docs Agent — GoldenGooseTees Documentation

## Activation: `@docs`

---

## DOCUMENTATION STRUCTURE

```
docs/
  goldengoosetees-knowledge.md       ← MASTER: architecture + API reference (keep current)
  ggt-multi-placement-addendum.md   ← Multi-placement flow + DB schema
  printful-console-setup.md         ← Printful token setup guide
  PRODUCTION_CHECKLIST.md           ← Pre/post deploy steps
  audit-report.md                   ← Generated by @builder during Phase 1 audit
README.md                           ← Quick start for new devs
.env.example                        ← All env vars documented
```

---

## KEEPING KNOWLEDGE DOCS CURRENT

After any architectural change:
1. Update `goldengoosetees-knowledge.md` — specifically:
   - Section 3 (DB schema) if tables change
   - Section 4 (Printful API) if endpoints or patterns change
   - Section 11 (Known Issues) — check off resolved items, add new ones
2. If placement flow changes: update `ggt-multi-placement-addendum.md`
3. If products/prices change: update `lib/config/products.config.ts` (self-documenting)

---

## PRODUCTION CHECKLIST TEMPLATE

```markdown
# GoldenGooseTees Production Checklist

## Environment Setup
- [ ] All env vars in Vercel Production scope
- [ ] Printful Private Token active, not expired
- [ ] Stripe webhook registered + secret matches STRIPE_WEBHOOK_SECRET
- [ ] Printful webhook registered + secret matches PRINTFUL_WEBHOOK_SECRET
- [ ] Supabase Storage buckets: 'designs' and 'assets' are Public

## Database
- [ ] Migrations applied (npx supabase db push)
- [ ] products_catalog seeded (npx ts-node scripts/seed-products.ts)
- [ ] RLS policies active

## Code
- [ ] npx tsc --noEmit → 0 errors
- [ ] npm run build → passes
- [ ] No PRINTFUL_API_KEY or STRIPE_SECRET_KEY in client files

## Post-Deploy Verification
- [ ] Landing page loads
- [ ] /studio loads, canvas initializes
- [ ] Create test design → upload → generate mockups → verify pending state
- [ ] Complete test purchase (Stripe test mode) → verify Printful order created
```
