/**
 * Placeholder: product data is curated via PRINTFUL_CURATED_PRODUCT_IDS and Printful API.
 * For a DB-backed products_catalog cache, implement inserts here after migrations exist.
 */
console.log('No local seed required — use PRINTFUL_CURATED_PRODUCT_IDS and npm run printful-resolve-launch')
