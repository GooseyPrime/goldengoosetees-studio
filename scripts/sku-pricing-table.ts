/**
 * Regenerate retail hints from live Printful data when pricing v2 + variant cache exist.
 * See docs/pricing.md and npm run printful-resolve-launch for catalog IDs.
 */
console.log('SKU pricing table: connect printful_variant_price_cache + estimate-costs (see docs/pricing.md)')
